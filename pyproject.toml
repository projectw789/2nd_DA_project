[project]
name = "job-application-analysis"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "projectw789", email = "projectw789@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
job-application-analysis = "job_application_analysis:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
